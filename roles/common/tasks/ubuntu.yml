---
- name: Ubuntu - Update and upgrade packages
  become: true
  apt:
    upgrade: dist
    update_cache: yes

- name: Ubuntu - Install base packages
  become: true
  package:
    name: "{{ item }}"
    state: present
  with_items:
  - make
  - ufw
  - unzip
